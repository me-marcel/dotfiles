# GNOME stow module

This module contains exported GNOME dconf settings in `dconf/gnome-profile.dconf`.

Use these helper scripts from the repo root:

- `./scripts/gnome-dconf-export.sh`
- `./scripts/gnome-dconf-import.sh`
